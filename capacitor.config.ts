import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raices.app',
  appName: 'raicesapp',
  webDir: 'dist/raicesapp/browser'
};

export default config;
